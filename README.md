# AI-Emoji-Story-Generator
1. Install Dependencies
pip install google.generativeai

2. Replace the placeholder API key in the script with your Google Gemini API key.

3. Run the Python script to generate emoji stories from a given prompt.
